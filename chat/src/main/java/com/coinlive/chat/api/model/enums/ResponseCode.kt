package com.coinlive.chat.api.model.enums

enum class ResponseCode {
    SUCCESS,
    FAIL,
    REGISTERED_NICKNAME,
    INVALID_NICKNAME,
    INVALID_TOKEN,
    INVALID_KEY,
    INVALID_STATUS,
    EXCEEDING_CERTIFICATIONS,
    UNKNOWN_MEMBER,
    DUPLICATED,
    PARAMETER_ERROR,
    TIMEOUT,
    PERMISSION_DENIED,
    NOT_SUPPORTED,
    NOT_FOUND,
    SERVER_ERROR,
    ALREADY_REGISTERED,
    UPLOAD_FAILED,
    PROFILE_UPLOAD
}